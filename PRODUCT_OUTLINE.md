# AI Production SOP Compliance Platform

## 1. 产品定义

**产品名称**：AI Production SOP Compliance Platform（工业 SOP 合规视觉检测平台）

**产品定位**：部署在工厂本地边缘计算环境中的生产质量与流程监督平台。系统融合工业相机、PLC、扫码枪、电批和传感器等证据，实时判断作业人员是否按既定 SOP 正确完成工序，并形成报警、视频留证和质量追溯闭环。

**不属于**：通用动作识别 Demo、云端视频分析服务，或安全 PLC / 安全回路的替代方案。

## 2. 业务目标

系统需要持续、可追溯地回答以下问题：

1. 当前正在执行哪个 SOP 步骤？
2. 步骤是否以足够证据真正完成？
3. 是否发生漏步骤、错顺序或超时？
4. 是否存在漏装、错装、位置或方向异常？
5. 异常发生时，为什么判定 NG？
6. 能否为每次异常和每个产品序列号保存可回放证据？

## 3. 目标用户与核心场景

| 用户 | 核心任务 |
| --- | --- |
| 产线员工 | 查看当前步骤、纠正明确的异常原因 |
| 班组长 / 质量人员 | 监控工位状态、处理报警、回放异常证据 |
| 工艺工程师 | 配置 SOP、步骤条件、超时、重试和设备绑定 |
| AI / 运维工程师 | 管理模型、阈值、相机、设备健康度及发布回滚 |
| MES / 质量系统 | 获取按 SN 关联的 PASS/FAIL、节拍、违规与证据 |

## 4. 产品边界

### 首期覆盖

- 固定相机、固定产品位、单人操作的装配或检测工位。
- 5 至 8 步明确 SOP。
- 视觉事件与扫码、PLC、电批或传感器事件融合。
- 实时步骤判断、漏步、错序、超时、PASS/FAIL 与异常留证。
- 车间局域网内的 Web 管理界面。

### 首期不覆盖

- 任意 SOP 文档的自动理解与零训练部署。
- 复杂多人协作、开放场景行为理解。
- AI 直接控制安全设备或替代安全回路。
- 未知异常的完全覆盖或跨工厂通用模型承诺。

## 5. 产品闭环

```text
相机 / PLC / 工具 / 扫码设备
        -> 实时采集与设备事件
        -> 视觉、动作和状态识别
        -> 标准化 Evidence Event
        -> SOP Rule Engine
        -> PASS / FAIL / 预警
        -> 报警、视频证据、数据库记录
        -> 工位监控、质量追溯、MES 集成
```

核心原则：AI 模块只产生带来源、时间和置信度的事件；SOP Engine 以确定性规则决定步骤状态和最终结果。

## 6. 核心产品能力

### 6.1 工位实时监控

- 展示实时视频、检测框、当前动作、当前步骤和执行进度。
- 以正常、等待/预警、NG、离线四种核心状态呈现工位健康度。
- 显示相机 FPS、AI 推理状态、当前周期和设备连接状态。

### 6.2 SOP 执行与规则引擎

- 支持线性 SOP 及后续扩展的分支、并行、返工、跳过和可选步骤。
- 管理 `WAITING`、`READY`、`RUNNING`、`COMPLETED`、`FAILED`、`SKIPPED`、`TIMEOUT` 状态。
- 基于依赖关系和完成条件识别漏步、错序、重复操作和超时。
- 每次判定必须输出可读的违规原因及其证据。

### 6.3 多源证据融合

- 视觉：目标、手部/工具、动作、零件与装配状态。
- 设备：PLC、扫码枪、电批、传感器、机器人和 MES。
- 每个步骤可配置 `all`、`any`、阈值、时间窗口及证据优先级。

### 6.4 异常与证据

- 在报警前后截取循环录像，生成原始视频、叠加视频、截图和事件 JSON。
- 报警中心按时间、工位、产品、SOP、步骤和异常类型检索。
- 单个 SN 可追溯完整步骤、设备结果、节拍、最终结果与视频证据。

### 6.5 配置与运营

- SOP 可配置步骤、依赖、完成条件、超时、重试、ROI 和异常策略。
- 相机可配置连接、分辨率、FPS、旋转、ROI、检测区和断线重连。
- 模型支持版本、部署范围、Shadow Mode、启用、回滚和审计。
- 设备与模型运行时具备健康监控和不可用状态，不以错误数据代替真实状态。

## 7. 系统形态与技术基线

**产品形态**：Web UI + 本地 Edge Runtime。所有生产判断在工厂局域网内完成，网络中断不影响本地运行；桌面 EXE 仅作为可选的 Tauri / WebView 外壳。

| 层级 | 选择 |
| --- | --- |
| 边缘部署 | Ubuntu + NVIDIA GPU + Docker |
| 视频与推理 | DeepStream 9.1 + TensorRT + GStreamer |
| AI 模型 | 目标/状态模型 + MMAction2；后续引入 STORM-PSR、StateDiffNet、AMNAR |
| 业务核心 | 自研 SOP Engine + Evidence Fusion |
| 设备集成 | OPC UA、Modbus TCP、TCP/UDP、Serial、MQTT、厂商 SDK |
| 服务层 | Python + FastAPI + WebSocket |
| 数据 | PostgreSQL 保存元数据；文件系统或对象存储保存视频证据 |
| 前端 | React + TypeScript；后续可采用 Ant Design、ECharts、React Flow |

环境版本必须锁定并可追溯：DeepStream、CUDA、TensorRT、驱动、模型、Engine 与 SOP 版本。

已核验的开源仓库、其产品角色、采用优先级与使用边界见 [GITHUB_REPOSITORIES.md](GITHUB_REPOSITORIES.md)。引用开源实现前仍需在采购或交付阶段逐项完成许可证、维护状态、模型权重与数据集使用条款审查。

## 8. 版本路线

### V1：单工位 MVP

目标：一个实际工位稳定运行，完成 `Camera -> Current Step -> PASS/FAIL -> Evidence` 闭环。

- 相机接入、实时视频、断线重连和运行时监控。
- 工件/工具/关键零件检测与关键动作识别。
- SOP 步骤状态机、漏步、错序和超时判断。
- PLC / 扫码枪 / 电批等至少一类设备事件接入。
- 工位详情页、报警中心、视频留证、PostgreSQL 元数据。

具体范围、仓库采用、服务设计、数据与模型、UI/UX、里程碑、验收和上线门槛见 [V1_IMPLEMENTATION_PLAN.md](V1_IMPLEMENTATION_PLAN.md)。

当前执行顺序先进行 `P0 Software Foundation`：以模拟 Adapter 完成软件平台、接口和 UI；真实相机、设备、模型与工位验收在后续 `M0-M6 Field Integration` 接入，不影响 P0 开发。

### V2：可复制的多工位产品

- 多工位与多相机管理、统一监控大盘。
- SOP 模板与图形化配置。
- 用户、审计、模型发布、Shadow Mode 和集中数据管理。
- MES 生产开始与生产结果双向集成。

### V3：高可靠性工艺验证

- STORM-PSR 判断“动作发生”之外的“步骤真正完成”。
- StateDiffNet 验证漏装、错装、位置和方向异常。
- 人工复核反馈进入困难样本和模型迭代闭环。

### V4：工厂级智能平台

- AMNAR 等异常检测、复杂 Task Graph、返工和分支工艺。
- 多工厂质量中心、模型中心、远程运维、节拍和缺陷关联分析。

## 9. V1 验收基线

| 维度 | 首期目标 |
| --- | --- |
| 视频 | 稳定实时播放，断线自动重连，离线不误判为 SOP FAIL |
| 步骤识别 | 普通步骤 F1 >= 90%，关键工序 Precision >= 95% |
| SOP 规则 | 在正确 Step Event 输入下，漏步/错序判断应为确定性正确 |
| 报警 | 初期误报少于 1 / 50 cycles，持续优化至少于 1 / 200 cycles |
| 延迟 | 普通异常在 1 至 2 秒内可见 |
| 追溯 | 每个 NG 有原因、事件、截图和前后录像；每个 SN 可查询结果 |

## 10. 成功标准与护城河

产品成功不以单个模型指标衡量，而以部署和复制效率衡量：一个新工位能够完成“创建站点、配置相机、画 ROI、导入 SOP、绑定设备、采样训练、Shadow 验证、上线”的标准化流程。

长期护城河是：

1. 可配置、可解释的 SOP Engine。
2. 视觉、PLC、扫码、扭矩和传感器构成的多证据融合机制。
3. 跨工艺的真实工业异常数据与人工复核闭环。
4. 快速部署、模型治理和按 SN 的质量追溯能力。

## 11. 后续产出顺序

本大纲确认后，建议依次形成：

1. V1 PRD 与试点工位范围说明。
2. SOP Schema、Event Contract 与 Rule Engine 状态机设计。
3. Edge Runtime / 服务边界 / API / 数据库技术设计。
4. 工位详情、报警中心、SOP 配置等 Web 信息架构与原型。
5. 数据采集、标注、训练、Shadow Mode 和验收测试计划。
